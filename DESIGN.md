# resume-tailor 设计系统（向导式重构 · 方向 A 清爽工作台）

> 唯一事实源。2026-09-07 用户在四方向预览中选定 A（拨盘 4/3/5），调整：修改步骤参考 diff 台、信息直击要点不说废话。

## 1. 视觉主题与氛围

本地单用户效率工具。Linear 式清爽工作台：亮底、克制、步骤轨 + 单步聚焦。零装饰性 webfont，一切为「下一步在哪、当前决策是什么」服务。氛围层：body 上 <6% 透明度的极淡渐变雾，可忽略但让底色不死板。

## 2. 色板与角色（DNA 供体：Linear light 模式中性阶）

| 角色 | 值 |
|---|---|
| 页面底 | `#f7f8f8` |
| 卡片面 | `#ffffff` |
| 边框 subtle / standard | `#e8e9ec` / `#d8dade` |
| 文字 主/次/弱 | `#18191b` / `#5c616b` / `#8a8f98` |
| 主操作按钮 | 近黑 `#18191b`，hover `#2e3033` |
| 成功/命中 | `#168a5f` / 底 `#e7f3ed` |
| 删除/未命中 | `#b3372e` / 底 `#fbeeed` |
| 警示/加分 | `#9a6d0c` / 底 `#fbf5e3` |
| diff 删 | 文字 `#a03528`，底 `#fbeeed` |
| diff 加 | 文字 `#1a6b4a`，底 `#e9f4ee` |
| focus 环 | `#18191b` 2px，offset 2px |

单重音色（近黑），语义色仅做状态。禁止蓝紫渐变、外发光、纯黑。

## 3. 排版规则

- 中文系统栈：`-apple-system, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans SC", sans-serif`
- 数字/计数一律 `font-variant-numeric: tabular-nums`
- 层级用字重（400/500/600/700）与颜色，不靠字号砸
- 步骤标题 17px/600；正文 14px；辅助 12px；micro 标签 11px + letter-spacing 0.8px（少量，≤3 处）
- 禁斜体（P-1）
- 禁装饰性中文 webfont（P-3）

## 4. 组件样式

- **步骤轨（左栏 176px）**：步骤项 28px 高，圆点编号 18px；done = 近黑实心，current = 白底 + 内描边 + 字重 600，future = 灰
- **结论摘要条**：`#f4f5f6` 底、8px 圆角、12px 字，一句话带出上一步结论
- **主按钮**：近黑底白字、7px 圆角、8px×18px padding；ghost = 透明底 + 边框；active `scale(0.97)`
- **diff 行**：`del` 红底划线弱化为纯色底（不删字）+ `ins` 绿底；理由行 12px 灰、`#` 前缀
- **关键词 chip**：命中绿底/未命中黄底，附「当前简历已命中 n/m」计数（服务端对 base 简历逐词计算）
- **五层解构（步骤 2）**：sec-label 分节；一句话结论 15px 加粗 balance；层徽标（维护灰/建设近黑/探索黄）+ 时间占比 tabular-nums；误读风险 = 黄底提示框；典型一天 = 灰底 narrative 框；验证问题列表 = 绿色问号前缀
- **VS 对照行**：两栏 grid（要求 | 现状），中间 1px 分隔；现状列 = 三态徽标（✓ 有 / ~ 部分 / ✗ 缺）+ 证据短语；<768px 塌缩为上下堆叠、虚线分隔
- **结论块**（步骤 3 顶部）：大号分数（tabular-nums）+ 判级徽标（值得投绿 / 改后投黄 / 观望橙 / 不建议投红，均为淡色底）+ 一句话判定依据；判级驱动底部按钮：负向判级主按钮为「换个 JD」，修改方案生成降为次级（省 token）
- **输入**：1px `#d8dade` 边框、6px 圆角，focus 品牌环

## 5. 布局原则

- 桌面：`grid-template-columns: 200px 1fr`，主体 max-width 680px（单步聚焦，禁止宽幅表格）
- 移动 <768px：步骤轨折为顶部水平进度条（点），单列
- 一步一屏：当前步之外的内容不可见；下一步按钮固定于步骤底部右侧
- 信息节奏：每步 ≤ 7 个条目；超过必须砍或折叠

## 6. 深度层级

卡片多层染色阴影：`0 0 0 1px #e8e9ec, 0 1px 2px rgba(24,25,27,.04), 0 4px 12px rgba(24,25,27,.04)`。浮层（如应用模式弹层）再叠 `0 12px 32px rgba(24,25,27,.12)`。

## 7. Do's / Don'ts

- Don't：一次性展开多个分析区块（本次重构核心病灶）
- Don't：卡片左侧竖线装饰（P-38）
- Don't：系统自言自语的状态文案常驻（P-14）
- Do：每步开头一句话说清这步在干什么、为什么
- Do：正文废话 ≤ 0；理由一句话
- Do：所有可编辑处带 focus-visible 品牌环

## 8. 响应式

`min-height: 100dvh`；<768px 步骤轨变横条、卡片 padding 收 16px、diff 行内标签换行不截断。

## 9. Motion 哲学

步骤切换：内容 `opacity 0→1 + translateY(6px→0)` 180ms `cubic-bezier(0.23,1,0.32,1)`；按钮按压 50ms 内 `scale(0.97)`；chip 勾选背景色 120ms 过渡。全部 `prefers-reduced-motion` 兜底。禁 ease-in、禁 >300ms、禁 transition: all。
